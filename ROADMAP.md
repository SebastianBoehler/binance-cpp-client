# Roadmap

- Add WebSocket API session support for user data streams without deprecated
  listen-key endpoints.
- Add typed helpers for account commission, rate-limit order count, and cancel
  replace.
- Add optional SBE market-data decoding as a separate module.
- Add futures clients as separate namespaces instead of mixing product paths into
  the Spot client.

